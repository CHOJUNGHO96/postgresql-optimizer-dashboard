# PostgreSQL Performance Analyzer - Environment Configuration

# Database URL for the optimizer's internal database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pg_optimizer

# Target PostgreSQL database to analyze (can be different from internal DB)
TARGET_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/your_database

# Redis URL for caching
REDIS_URL=redis://localhost:6379

# Secret key for JWT tokens (change in production!)
SECRET_KEY=your-secret-key-change-in-production

# Frontend API URL
VITE_API_URL=http://localhost:8000
